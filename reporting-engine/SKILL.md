---
name: reporting-engine
description: Ostorlab reporting engine — the Django + Graphene (GraphQL) backend for vulnerability, scan, and asset management (`ogle_reporting_engine`). Load when working on its architecture and apps, the GraphQL API and schema composition, the domain models (Scan, Vulnerability, KnowledgeBase, Fingerprint), organisations / permissions / authentication, integrations, Airflow jobs, scanners, or the scan / agent-group pipeline.
---

# Reporting engine

The Ostorlab vulnerability-management backend (`ogle_reporting_engine`).

Start here: [../../content/reporting-engine/index.md](../../content/reporting-engine/index.md). It maps
the component's sub-areas — follow its links to the specific doc you need.
